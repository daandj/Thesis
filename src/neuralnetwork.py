import keras
import numpy as np

# Implement here the neural network class that drives the AlphaGo Zero
# tree search.
class NeuralNetwork:

    # Run the network on the inputs, returns the value and move probabilities.
    #TODO: Check that the output of a network is indeed a numpy array (and if 
    # maybe a normal array might be better).
    def predict() -> tuple[int, np.array]:
        pass

